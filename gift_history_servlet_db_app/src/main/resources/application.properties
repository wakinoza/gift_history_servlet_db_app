# ===================================================================
# 1. アプリケーション基本設定
# ===================================================================

spring.application.name=gift-management-app


# ===================================================================
# 2. JPA / Hibernate 設定
# ===================================================================
spring.jpa.hibernate.ddl-auto=none

spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl

# ===================================================================
# 3. セッション / クッキー安全設定（Servlet時代のSet-Cookie手動書き換えの自動化）
# ===================================================================
server.servlet.session.cookie.http-only=true
server.servlet.session.cookie.secure=false
server.servlet.session.cookie.same-site=lax

# ===================================================================
# 4. セキュリティヘッダー設定（Servlet時代のSecurityHeaderFilterの自動化）

spring.mvc.contentnegotiation.media-types.html=text/html;charset=UTF-8
server.servlet.encoding.charset=UTF-8
server.servlet.encoding.force=true
